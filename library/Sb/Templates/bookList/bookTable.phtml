<?php if (!$emptyList) { ?>
<div class="navigation">
    <div class="inner-padding">
        <div class="lln-search">
            <div class="llns-label"><?php _e("Rechercher", "s1b");?></div>
            <div class="search-field">
                <form action="" method="GET" name="listSearchForm">
                    <input type="hidden" name="key" value="[@key]" />
                    <input type="hidden" name="page" value="<?php echo \Sb\Entity\LibraryPages::BOOK_LIST;?>" />
                    <input type="text" class="search-field-input searchTerm" id="listSearchValue" name="searchvalue" value="[@listSearchValue]" onclick="if (this.value == '[@listSearchDefValue]') {this.value = ''};"/>
                    <button class="searchBtn search-button" ></button>
                </form>
            </div>
            <div class="lln-filter">
                <form action="" method="GET" id="listFilterForm">
                    <input type="hidden" name="key" id="" value="[@key]" />
                    <input type="hidden" name="filter" id="listFilterFormFilter" value="" />
                    <input type="hidden" name="filtertype" id="listFilterFormFilterType" value="" />
                    <select id="authorsFirstLetterSelect" class="input-item margin-left">
                        <option value=""><?php _e("Filtrer par auteur","s1b");?></option>
                        <?php foreach ($authorsFirstLetters as $authorFirstLetter) {echo "<option " . ($selectedAuthorLetter == $authorFirstLetter ? "selected" : "") . " value=\"" . $authorFirstLetter . "\">" . $authorFirstLetter . "</option>";}?>                        
                    </select>
                    <select id="titlesFirstLetterSelect" class="input-item margin-left">
                        <option value=""><?php _e("Filtrer par titre","s1b");?></option>
                        <?php foreach ($titlesFirstLetters as $titleFirstLetter) {echo "<option " . ($selectedTitleLetter == $titleFirstLetter ? "selected" : "") . " value=\"" . $titleFirstLetter . "\">" . $titleFirstLetter . "</option>";}?>                        
                    </select>
                </form>
            </div>
        </div>
        <div class="nav-links pageNav library" key="allBooks">
            [@links]
        </div>
        <div class="nav-resume">
            <?php _e("Livres [@first] à [@last] sur [@nbItemsTot]", "s1b") ?>
        </div>
    </div>
</div>

<div class="ll-list">
    <table cellspacing="0" cellpadding="0">
        [@tableHeader]
        [@booksToShow]
    </table>
</div>

<div class="navigation">
    <div class="nav-links pageNav library" key="allBooks">
        [@links]
    </div>
</div>
<?php } else {?>
<div class="ll-nobooks">
<?php _e("Aucun livre...","s1b");?>
</div>
<?php } ?>
