<?php
use Sb\Helpers\StringHelper;
?>
<div class="user-box">
    <div class="js_userLastEvents ub-lastevents">
        <div class="ub-title-wrap">
            <div class="ub-title">                
                <?php echo sprintf(__("Activités de %s", "s1b"), StringHelper::tronque(ucwords($user->getFirstName()), 14)); ?>
            </div>
        </div>
        <div class="ub-content">
            <?php if (count($events) > 0) { ?>
            <div class="ubc-part">
                
                    <?php
                    foreach ($events as $event) {
                        $event = new Sb\View\UserEvent($event, false);
                        echo $event->get();
                    }
                    ?>
                
            </div>
            <?php if (count($events) > 3) {?>
            <div class="see-more">
                <div class="inner-padding">
                    <a class="link lnkCollapseExpand" href="#"></a>
                </div>
            </div>
            <?php } ?>
            <?php } else { ?>
            <div class="ubc-part">
                <?php echo sprintf(__("%s n'a pas d'activité.", "s1b"), ucwords($user->getFirstName())); ?>                
            </div>
            <?php }?>
        </div>        
    </div>
</div>